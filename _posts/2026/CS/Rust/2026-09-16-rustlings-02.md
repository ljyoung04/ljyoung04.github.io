---
title: Rustlings - 02
date: 2026-09-16 16:17:34 +0900
categories: [CS, Rust]
---

## 1.

```rust
// TODO: Add some function with the name `call_me` without arguments or a return value.

fn main() {
    call_me(); // Don't change this line
}
```

call_me가 정의되어 있지 않다. `fn call_me(){}`

## 2.

```rust
// TODO: Add the missing type of the argument `num` after the colon `:`.
fn call_me(num) {
    for i in 0..num {
        println!("Ring! Call number {}", i + 1);
    }
}

fn main() {
    call_me(3);
}
```

함수 시그니처에서 각 매개변수의 타입을 반드시 선언해야한다. `fn call_me(num : i32)`

## 3.

```rust
fn call_me(num: u8) {
    for i in 0..num {
        println!("Ring! Call number {}", i + 1);
    }
}

fn main() {
    // TODO: Fix the function call.
    call_me();
}
```

함수 시그니처에 매개변수가 존재한다면 호출할 때 반드시 인자를 전달해야한다.

## 4.

```rust
// This store is having a sale where if the price is an even number, you get 10
// Rustbucks off, but if it's an odd number, it's 3 Rustbucks off.
// Don't worry about the function bodies themselves, we are only interested in
// the signatures for now.

fn is_even(num: i64) -> bool {
    num % 2 == 0
}

// TODO: Fix the function signature.
fn sale_price(price: i64) -> {
    if is_even(price) {
        price - 10
    } else {
        price - 3
    }
}

fn main() {
    let original_price = 51;
    println!("Your sale price is {}", sale_price(original_price));
}
```

sale_price의 반환값 타입을 지정해주지 않았다. 전달되는 price가 i64이기 때문에 i64로 지정해주면 된다. `-> i64`

## 5. 

```rust
// TODO: Fix the function body without changing the signature.
fn square(num: i32) -> i32 {
    num * num;
}

fn main() {
    let answer = square(3);
    println!("The square of 3 is {answer}");
}
```

세미콜론을 때야지 표현식으로 간주되어 해당 값을 반환하게 된다.
`num * num`